import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '../components/Header'
import Footer from '../components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'WaddleAI - AI Proxy & Management Platform',
  description: 'Enterprise AI proxy with OpenAI-compatible APIs, advanced routing, security, and token management',
  keywords: 'AI, proxy, OpenAI, API, management, enterprise, security, routing',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}