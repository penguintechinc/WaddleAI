mkdocs>=1.6.0
mkdocs-material>=9.6.0
pymdown-extensions>=10.0.0
mkdocs-mermaid2-plugin>=1.2.3
# mkdocs-mermaid2-plugin needs setuptools (pkg_resources) to build/run;
# uv resolves it transitively today but Dependabot's independent resolver
# does not, silently dropping the == pin and breaking
# `pip install --require-hashes`. Declared explicitly so it is always kept.
setuptools==84.0.0
